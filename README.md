now we added all the routes with a navbar at top
![alt text](image.png)


##git command needed

Before pulling, check status:

###git status

Then fetch remote updates:

###git fetch

See difference:(optional)

###git log --oneline --graph --all

Then pull:

###git pull origin main